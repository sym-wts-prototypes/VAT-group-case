{
  "name": "wts-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "storybook": "pnpm --filter @wts/ui storybook",
    "build:storybook": "pnpm --filter @wts/ui build:storybook",
    "gallery": "pnpm --filter @wts/gallery dev",
    "gen:prototype": "node scripts/new-prototype.mjs",
    "assemble": "node scripts/assemble-dist.mjs",
    "preview": "pnpm --filter @wts/worker dev",
    "deploy": "pnpm --filter @wts/worker deploy"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
